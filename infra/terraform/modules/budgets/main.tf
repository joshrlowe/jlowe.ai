# modules/budgets — skeleton (later phase)
#
# Cost guardrails for the account.
#
# Planned resources:
#   - aws_budgets_budget (monthly cost) with notification thresholds
#   - aws_sns_topic + email subscription for alerts
#
# Planned variables: monthly_limit_usd, alert_email
# Planned outputs:    budget_id
#
# TODO(budgets-phase): implement.
