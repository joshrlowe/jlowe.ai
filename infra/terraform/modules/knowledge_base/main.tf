# modules/knowledge_base — skeleton (later phase)
#
# Vector index over corpus/ for digital-twin retrieval.
#
# Planned resources:
#   - aws_s3_bucket for ingested corpus documents
#   - Bedrock Knowledge Base + data source, OR an S3 Vectors index
#     (verify regional availability before committing — standing rule)
#   - aws_iam_role for ingestion + query
#
# Planned variables: environment, embedding_model_id
# Planned outputs:    knowledge_base_id, index_arn
#
# TODO(kb-phase): implement.
