# dependencies / builds
node_modules/
.next/
out/
dist/
coverage/
.lighthouseci/
*.tsbuildinfo
bundle-budget-report.json

# env & secrets — never commit
.env*
!.env.example

# OS / editor
.DS_Store

# terraform
infra/terraform/**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log

# raw asset sources — build inputs, never shipped (keep the dir, not its contents)
packages/asset-pipeline/raw-assets/*
!packages/asset-pipeline/raw-assets/.gitkeep
!packages/asset-pipeline/raw-assets/README.md

# local tooling & working artifacts
.claude/
.cursor/
.audit-*/
verification-*/
VERIFICATION_REPORT.md
*.log

# Blender cinematic pipeline outputs (frames, blends)
tools/cinematic/out/
__pycache__/
